-- Jalankan sekali di Supabase → SQL Editor (setelah reset sandi hanya lewat Supabase Auth).
-- Menghapus tabel alur lama yang tidak dipakai aplikasi lagi.

DROP TABLE IF EXISTS password_reset_tokens;
